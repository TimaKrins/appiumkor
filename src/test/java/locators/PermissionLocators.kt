package locators

import constructor_classes.LocatorsConstructor

class PermissionLocators {
    val permissionAllowButtonOnPermissionLocators = LocatorsConstructor(
        androidId = "com.android.packageinstaller:id/permission_allow_button"
    )
    // Согласие на геолокацию перед выбором города при запуске МП
}