package locators

import constructor_classes.LocatorsConstructor

class ProposedCityLocators {
    val proposedCityButtonOnProposedCity = LocatorsConstructor(
        androidId = "android:id/button1"
    )
    // кнопка ДА с предложенным городом на экране выбора города при первом запуске
}
